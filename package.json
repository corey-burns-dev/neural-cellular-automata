{
  "name": "neural-cellular-automata",
  "version": "1.0.0",
  "description": "Neural Cellular Automata (NCA) 'living' texture generator",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "bun run build && bunx wrangler pages deploy dist --project-name neural-cellular-automata",
    "deploy:production": "bun run build && bunx wrangler pages deploy dist --project-name neural-cellular-automata"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "typescript": "^5.9.3"
  }
}
